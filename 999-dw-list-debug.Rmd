---
title: "R Notebook"
output: html_notebook
---

# Bug: list charts doesn't work

> https://github.com/munichrocker/DatawRappr/issues/65

```{r}
x <- list(
  list(publicId = "wuTKT", folderId = NULL)
)
DatawRappr:::bind_rows(x)
```

```{r}
x <- list(
  list(publicId = "wuTKT", folderId = NA, thumbnails = list(full = "foo", plain = "bar"))
)
DatawRappr:::bind_rows(x)
```

